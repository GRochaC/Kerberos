[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

addopts = "-v --disable-warnings"

log_cli = true
log_cli_level = "INFO"
log_cli_format = "%(asctime)s | %(levelname)-8s | %(name)-15s | %(filename)s:%(lineno)d | %(message)s"
log_cli_date_format = "%H:%M:%S"